# Sample documentation content (Markdown text)
documentation = """
# Project Documentation

## Introduction
This project automates the deployment pipeline for the system. It includes a CI/CD setup using Jenkins, GitHub Actions, and Docker.

## Setup
2. Clone the repository.
3. Install dependencies.
4. Run `docker-compose up` to start the application.

## Best Practices
- Ensure all tests pass before merging any pull requests.
- Use Docker for local development to ensure consistency across environments.

## Troubleshooting
If you encounter issues with the Docker containers, try restarting the service using the following command:
```bash
docker-compose restart

"""
